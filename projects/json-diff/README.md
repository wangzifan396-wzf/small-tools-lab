# JSON 结构差异 (json-diff)

零依赖的 JSON 结构比较工具。在浏览器里逐路径对比，数据不出本机。

## 功能
- 把两个 JSON 拍平为「路径 → 值」（支持嵌套对象与数组下标），逐项比较。
- 标注三类变化：新增（绿色 +）、删除（红色 −）、修改（橙色 ~，显示旧→新）。
- 统计新增 / 删除 / 修改 / 相同数量，一键复制差异报告。
- 解析失败给出可读报错，不会整页崩溃。

## 用法
打开 `index.html`，分别粘贴 JSON A 与 JSON B，点「比较」。

适合核对接口返回、配置变更、数据迁移前后差异。纯前端、无网络、无依赖。
