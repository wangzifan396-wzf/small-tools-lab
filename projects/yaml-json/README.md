# YAML ⇄ JSON

YAML（block 风格子集）与 JSON 互转。

- 支持嵌套映射、序列（`- item`）、序列内映射、内联 `[ ]` / `{ }` 与基础标量（字符串 / 数字 / 布尔 / null）
- JSON → YAML 产出 2 空格缩进的 block 风格，含必要引号与字符串转义
- 实时转换，解析错误就地提示

纯浏览器零依赖，双击 `index.html` 即可运行。
