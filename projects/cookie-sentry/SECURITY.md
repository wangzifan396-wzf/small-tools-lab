# Security

Input headers remain in browser memory and are rendered with `textContent`. Do not paste production cookie values into shared screens or exported reports; values are intentionally retained in the local page while it is open.
