# Contributing

Run `npm test` and `npm run check` before opening a pull request. Add generator tests for changes to Markdown structure. Keep preview rendering state-driven and do not inject arbitrary Markdown as HTML.

Interface changes should be checked with keyboard navigation and at widths near 390 px and 1440 px.
