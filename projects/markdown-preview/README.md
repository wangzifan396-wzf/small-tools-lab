# Markdown 预览

轻量 Markdown 渲染器（子集）。

- 支持标题、粗体 / 斜体、行内代码与围栏代码、有序 / 无序列表、引用、链接、分割线
- 输出已做 HTML 转义与链接协议校验（仅允许 http/https/mailto/相对路径），可安全预览
- 输入时自动渲染，可复制生成的 HTML

注：不支持表格、脚注、嵌套列表等复杂语法。纯浏览器零依赖，双击 `index.html` 即可运行。
