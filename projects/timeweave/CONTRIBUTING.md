# Contributing

Run `npm test` and `npm run check` before opening a pull request. Add focused tests for time-zone or calendar changes, including a date and expected UTC instant. Avoid hard-coded numeric offsets in application behavior.

Interface changes should be checked with keyboard navigation and at widths near 390 px and 1440 px.
