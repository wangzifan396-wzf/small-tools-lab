# Security Policy

Report vulnerabilities through the repository host's private security advisory feature. Plans are encoded in URLs, so users should avoid placing confidential meeting details in links. Reports involving generated calendar injection or unsafe URL parsing are especially useful.
