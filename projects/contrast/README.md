# 对比度检查 (contrast)

零依赖的 WCAG 2.1 颜色对比度检查器。在浏览器里计算，数据不出本机。

## 功能
- 输入前景色（文字）与背景色，计算相对亮度对比度比值。
- 同时判定 AA / AAA 在「普通文字 / 大号文字」下是否通过。
- 实时预览文字效果，支持 HEX（`#RGB` / `#RRGGBB`）与 `rgb()` / `rgba()`。
- 颜色选择器与文本框双向同步。

## 用法
打开 `index.html`，选好或输入两种颜色，点「计算对比度」。

适合检查网页可访问性（a11y）、确认设计稿文字可读性。纯前端、无网络、无依赖。
