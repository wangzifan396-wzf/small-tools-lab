# Regex 测试器

纯前端、零依赖的正则表达式实时测试工具。

## 功能
- 输入正则（不含外层的 `/ /`）与 flags（`g i m s u y`），实时匹配测试文本。
- 在原文中**高亮**所有匹配片段（`<mark>`）。
- 列出每一处匹配的**位置**与**捕获组**（组 1、组 2 …）。
- 正则语法错误即时提示，不会崩溃。

## 用法
打开 `index.html`，分别填写正则、flags、测试文本即可。

## 适用场景
- 调试爬虫 / 日志提取用的正则。
- 验证邮箱、日期、URL 等模式。
- 教学演示捕获组。

数据不出本机，无需联网。
