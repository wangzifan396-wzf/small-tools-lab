// Public API for text-forge. Works in Node (ESM) and the browser.
export {
  slugify,
  toCase,
  normalizeUnicode,
  removeDiacritics,
  width,
  toFullWidth,
  toHalfWidth,
  cleanWhitespace
} from './core/forge.js';
