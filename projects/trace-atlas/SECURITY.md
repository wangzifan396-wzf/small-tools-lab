# Security

Trace data stays in browser memory and is rendered with `textContent`. No trace backend, URL, or external resource is contacted. Exported reports may contain service names, span names, and error details; review them before sharing.
