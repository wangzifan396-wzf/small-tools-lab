# Contributing

Add captured command output as a minimal synthetic fixture, not a dump from a real machine. Every platform parser needs a positive and malformed-input test. Inventory commands must remain fixed, read-only, and shell-free.
