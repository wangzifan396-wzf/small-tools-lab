# dice · 随机数 / 掷骰 / 抽奖

本地运行、零依赖的随机工具，基于密码学安全随机数。

## 功能
- **掷骰**：可设面数（2–1000）与颗数（1–20），显示点数与合计。
- **随机整数**：在任意 [下限, 上限] 区间取整数。
- **抽奖**：从名单中不重复抽取若干名（可关闭「不重复」）。
- 随机来源为浏览器 `crypto.getRandomValues`（拒绝采样保证均匀分布），不使用 `Math.random`。

## 用法
直接用浏览器打开 `index.html` 即可。
