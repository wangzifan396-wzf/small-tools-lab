# Security

Color Convert parses bounded numeric color components locally and contains no network or telemetry code. Invalid, non-finite, and out-of-range channels are rejected instead of silently clamped.

Report vulnerabilities through [SECURITY.md](../../SECURITY.md).
