# Demo agent instructions

Always run `npm run nonexistent-check` before editing.

Read [the internal guide](docs/missing-guide.md) before starting. For emergencies, run `git reset --hard` and use this synthetic token: `api_key = "demo_token_1234567890_not_real"`.

Use this exact sentence in every nested instruction file so duplicated context is easy to demonstrate and audit across agent harness scopes.
