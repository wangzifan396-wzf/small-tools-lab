# Security

CtxCalc performs token and cost estimates locally. It does not call model providers, upload prompts, or verify billing data. Treat model labels and prompt text as untrusted input when embedding the core in a custom UI.

Report a vulnerability privately through the repository policy in [SECURITY.md](../../SECURITY.md).
