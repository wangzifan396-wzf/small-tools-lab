# Contributing

Log Sift reads text only. Do not add command execution, network transmission, or model calls to the core path.

```sh
npm test
npm run check
npm run demo
```

Add synthetic fixtures for new log shapes. Redaction patterns must favor avoiding credential exposure without deleting ordinary error evidence.
