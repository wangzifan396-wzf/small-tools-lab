# Contributing

Keep parsing deterministic and offline. Never execute package scripts or fetch dependency content as part of analysis.

```sh
npm test
npm run check
npm run demo
```

Add a focused fixture for every new source type or finding rule. Reports must escape repository-controlled values.
