# Security

Radix is a local, dependency-free base converter. It does not send input over the network or execute converted values. Treat output as data when embedding the core in another application.

Report a vulnerability privately through the repository policy in [SECURITY.md](../../SECURITY.md).
