# Contributing

Rules must be deterministic, documented, and covered by both a positive and a negative fixture. Avoid executing samples or making network requests. Prefer a narrow high-confidence expression over a broad noisy one, and run `npm test` before opening a pull request.
