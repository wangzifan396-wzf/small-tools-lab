# Contributing

Create a focused branch, add tests for inference changes, and run `npm test` plus `npm run check` before opening a pull request. Keep source data local and discuss any new runtime dependency before adding it.

Interface changes should be checked with keyboard navigation and at widths near 390 px and 1440 px.
